1. **事务的四个条件**
   - 原子性：一个事务（transaction）中的所有操作，要么全部完成，要么全部不完成，不会结束在中间某个环节。事务在执行过程中发生错误，会被回滚（Rollback）到事务开始前的状态，就像这个事务从来没有执行过一样。
   - 一致性：在事务开始之前和事务结束以后，数据库的完整性没有被破坏。这表示写入的资料必须完全符合所有的预设规则，这包含资料的精确度、串联性以及后续数据库可以自发性地完成预定的工作。
   - 隔离性：数据库允许多个并发事务同时对其数据进行读写和修改的能力，隔离性可以防止多个事务并发执行时由于交叉执行而导致数据的不一致。事务隔离分为不同级别，包括读未提交（Read uncommitted）、读提交（read committed）、可重复读（repeatable read）和串行化（Serializable）。
   - 持久性：事务处理结束后，对数据的修改就是永久的，即便系统故障也不会丢失。
2. **聚集索引和二级索引**
   - 一定会有聚集索引：如果表结构中定义了主键，聚集索引就根据主键建立，否则如果有唯一列就用唯一列，否则就会自动在每条记录中生成一个隐藏的ID列并以此建立聚集索引，聚集索引的叶子节点就是一行行真实的数据。
   - 二级索引：叶子节点是索引列加ID。
3. **插入记录存在则无视，需要有主键或唯一索引**
   - insert ignore into ...
4. **插入记录存在则更新，需要有主键或唯一索引**
   - insert into table_name(colums...) values(values...) on duplicate key update colum = value...
   - replace into (与insert into语法一致，且是原子操作；update本质上先删除后插入，不是原子操作)
5. **复杂sql查询**
6. **锁**